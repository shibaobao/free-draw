const Tool = require('./tool');
const Basic = require('./basic');

class Rect extends Basic {
  constructor (options) {
    super(options);
    this.initRect();
  }

  initRect () {

  }
}

module.exports = Rect;